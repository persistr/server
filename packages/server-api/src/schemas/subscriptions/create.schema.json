{
    "type": "object",
    "properties": {
        "adapter": {
            "description": "Subscription adapter",
            "type": "object"
        },
        "filter": {
            "description": "Subscription filter",
            "type": "object"
        }
    },
    "required": [ "adapter", "filter" ]
}
